spring:
  cloud:
    config:
      #定义为config-server 文件的名字
      name: configClient
      #去获取到配置中心的地址
      uri : http://localhost:5000/
      #对应security的账号和密码
#      username: sanleny
#      password: 123456
  ##使用的环境配置
  profiles:
    active: dev
eureka:
  client:
    ##禁止注册到server上
    enabled: false